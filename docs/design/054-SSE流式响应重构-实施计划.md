# SSE 流式响应重构 - 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 SSE 流式响应问题，使用 aggo 成熟的 SSE 包重构 langgraph_handler.go

**Architecture:**
- 复制 aggo sse 包到 pkg/sse/
- 创建 pkg/sse/deerflow/ 扩展层
- 重构 langgraph_handler.go 使用新的 SSE Writer

**Tech Stack:** Go, Gin, Eino, zap, sonic

---

## 文件结构

| 文件 | 操作 | 职责 |
|------|------|------|
| `pkg/sse/sse.go` | 创建 | SSE 常量和辅助函数 |
| `pkg/sse/event.go` | 创建 | Event 结构和对象池 |
| `pkg/sse/writer.go` | 创建 | SSE Writer 核心实现 |
| `pkg/sse/deerflow/writer.go` | 创建 | DeerFlow 专用 Writer |
| `internal/api/langgraph_handler.go` | 修改 | 重构使用新 SSE 包 |

---

## 任务列表

### Task 1: 复制 aggo sse 包

**Files:**
- Create: `pkg/sse/sse.go`
- Create: `pkg/sse/event.go`
- Create: `pkg/sse/writer.go`

- [ ] **Step 1: 创建 pkg/sse 目录**
  ```bash
  mkdir -p /Users/leven/space/hein/deepmind/pkg/sse
  ```

- [ ] **Step 2: 复制 sse.go**
  从 `test/aggo/pkg/sse/sse.go` 复制内容，修改包名为 `package sse`

- [ ] **Step 3: 复制 event.go**
  从 `test/aggo/pkg/sse/event.go` 复制内容，修改包名为 `package sse`

- [ ] **Step 4: 复制 writer.go**
  从 `test/aggo/pkg/sse/writer.go` 复制内容，修改包名为 `package sse`，更新 import 路径

- [ ] **Step 5: 验证编译通过**
  ```bash
  go build ./pkg/sse
  ```
  Expected: 无错误

---

### Task 2: 创建 DeerFlow 扩展层

**Files:**
- Create: `pkg/sse/deerflow/writer.go`

- [ ] **Step 1: 创建 pkg/sse/deerflow 目录**
  ```bash
  mkdir -p /Users/leven/space/hein/deepmind/pkg/sse/deerflow
  ```

- [ ] **Step 2: 编写 deerflow/writer.go**

  ```go
  package deerflow

  import (
      "net/http"
      "github.com/weibaohui/nanobot-go/pkg/sse"
      "github.com/bytedance/sonic"
  )

  // Writer DeerFlow 专用 SSE Writer
  type Writer struct {
      base *sse.Writer
  }

  // NewWriter 创建 DeerFlow SSE Writer
  func NewWriter(id string, w http.ResponseWriter) *Writer {
      base := sse.NewWriter(id, w)
      if base == nil {
          return nil
      }
      return &Writer{base: base}
  }

  // WriteMessagesTuple 发送 messages-tuple 事件
  func (w *Writer) WriteMessagesTuple(msg interface{}) error {
      return w.base.WriteEventJSON("", "messages-tuple", []interface{}{msg})
  }

  // WriteValues 发送 values 事件
  func (w *Writer) WriteValues(values map[string]interface{}) error {
      return w.base.WriteEventJSON("", "values", values)
  }

  // WriteEnd 发送 end 事件
  func (w *Writer) WriteEnd(usage map[string]interface{}) error {
      data := map[string]interface{}{
          "usage": usage,
      }
      return w.base.WriteEventJSON("", "end", data)
  }

  // Close 关闭 Writer
  func (w *Writer) Close() {
      w.base.Close()
  }

  // IsClosed 检查是否已关闭
  func (w *Writer) IsClosed() bool {
      return w.base.IsClosed()
  }

  // SetDone 设置完成回调
  func (w *Writer) SetDone(f func(http.ResponseWriter) error) {
      w.base.SetDone(f)
  }
  ```

- [ ] **Step 3: 验证编译通过**
  ```bash
  go build ./pkg/sse/deerflow
  ```
  Expected: 无错误

---

### Task 3: 重构 langgraph_handler.go - 第一部分（准备）

**Files:**
- Modify: `internal/api/langgraph_handler.go`

- [ ] **Step 1: 添加 import**
  在文件顶部 import 区域添加：
  ```go
  "github.com/weibaohui/nanobot-go/pkg/sse/deerflow"
  ```

- [ ] **Step 2: 备份当前 streamRun 函数（可选但推荐）**
  复制当前 `streamRun` 函数内容，注释为 `// OLD IMPLEMENTATION` 作为备份

---

### Task 4: 重构 langgraph_handler.go - 第二部分（简化 SSE 发送）

**Files:**
- Modify: `internal/api/langgraph_handler.go:633-966` (streamRun 函数)

- [ ] **Step 1: 替换 sendEvent 闭包为 deerflow.Writer**

  删除旧的 sendEvent 闭包（约 L688-693），替换为：
  ```go
  // 创建 DeerFlow SSE Writer
  sseWriter := deerflow.NewWriter(runID, w)
  if sseWriter == nil {
      c.JSON(http.StatusInternalServerError, gin.H{"error": "当前响应不支持 SSE"})
      return
  }
  defer sseWriter.Close()
  ```

- [ ] **Step 2: 替换所有 sendEvent 调用**
  - `sendEvent("messages-tuple", ...)` → `sseWriter.WriteMessagesTuple(...)`
  - `sendEvent("values", ...)` → `sseWriter.WriteValues(...)`
  - `sendEvent("end", ...)` → `sseWriter.WriteEnd(...)`

---

### Task 5: 重构 langgraph_handler.go - 第三部分（简化流式读取）

**Files:**
- Modify: `internal/api/langgraph_handler.go:768-840`

- [ ] **Step 1: 简化流式读取逻辑**
  参考 eino 的 sse.go 实现，简化 buffer 累积逻辑：

  ```go
  // 参考 test/Agent-Eino 的实现：使用 buffer 累积 delta
  buffer := ""
  flushThreshold := 3 // 每 3 字符就发送，更实时
  chunkCount := 0

  for {
      // 检查客户端是否断开
      select {
      case <-c.Request.Context().Done():
          h.logger.Debug("客户端断开连接")
          return
      default:
      }

      chunk, recvErr := streamReader.Recv()
      chunkCount++

      if recvErr != nil {
          // 检查是否是流结束
          if recvErr == io.EOF || strings.Contains(recvErr.Error(), "EOF") || strings.Contains(recvErr.Error(), "end of stream") {
              h.logger.Debug("流式响应读取完毕", zap.Int("totalChunks", chunkCount))
              // 发送剩余的 buffer
              if buffer != "" {
                  aiResponse.WriteString(buffer)
                  partialMsg := map[string]interface{}{
                      "type":    "ai",
                      "id":      aiMessageID,
                      "content": aiResponse.String(),
                  }
                  sseWriter.WriteMessagesTuple(partialMsg)
              }
          } else {
              h.logger.Error("流式响应读取错误", zap.Error(recvErr), zap.Int("chunkCount", chunkCount))
          }
          break
      }

      if chunk == nil {
          continue
      }

      h.logger.Debug("[StreamReader] received chunk",
          zap.Int("chunkNum", chunkCount),
          zap.String("role", string(chunk.Role)),
          zap.Int("contentLen", len(chunk.Content)),
          zap.Int("toolCallsCount", len(chunk.ToolCalls)),
          zap.String("content", chunk.Content))

      // 处理工具调用（暂略）
      if len(chunk.ToolCalls) > 0 {
          continue
      }

      if chunk.Content == "" {
          continue
      }

      // 关键：delta 追加，不是替换！
      buffer += chunk.Content

      // 更实时地发送：每 3 字符或遇到标点就发送
      if len(buffer) >= flushThreshold || strings.Contains(buffer, "。") || strings.Contains(buffer, "\n") || strings.Contains(buffer, "！") || strings.Contains(buffer, "？") {
          aiResponse.WriteString(buffer)

          partialMsg := map[string]interface{}{
              "type":    "ai",
              "id":      aiMessageID,
              "content": aiResponse.String(),
          }
          sseWriter.WriteMessagesTuple(partialMsg)

          buffer = ""
      }
  }
  ```

- [ ] **Step 2: 删除回退模拟流式逻辑**
  删除 `if !useRealStream { ... }` 整个代码块（约 L848-910）

---

### Task 6: 验证编译和测试

**Files:**
- Modify: `internal/api/langgraph_handler.go`

- [ ] **Step 1: 运行编译检查**
  ```bash
  go build ./internal/api
  ```
  Expected: 无错误

- [ ] **Step 2: 运行完整项目编译**
  ```bash
  make build
  ```
  Expected: 无错误

- [ ] **Step 3: 手动测试（可选但推荐）**
  启动服务，用前端或 curl 测试 SSE 端点

---

### Task 7: 编写功能总结文档

**Files:**
- Create: `docs/design/054-SSE流式响应重构-实施总结.md`

- [ ] **Step 1: 编写实施总结文档**
  内容包括：
  - 实现了什么
  - 与需求的对应关系
  - 关键实现点
  - 已知限制或待改进点

---

## 验收标准

- [ ] 项目可正常编译
- [ ] SSE 流式响应前端可正常接收
- [ ] 连接稳定，不会无故断开
- [ ] 正确处理客户端断开连接
